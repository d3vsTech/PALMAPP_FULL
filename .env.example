# ── PalmApp — Root env ────────────────────────────────────────────────────────
# Variables de entorno del proyecto raíz (monorepo)
# Cada sub-proyecto tiene su propio .env.example

# Entorno: development | production
NODE_ENV=development
